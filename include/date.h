/**
 * Either updates the date or simply prints the current date
 * @param args Unparsed date arguments to update the system date
 */
void date(char* args);

/**
 * Prints the current date
 * @return The current date as a string
 */
char *getdate();
