/**
 * Either updates the date or simply prints the current date
 * @param args Unparsed date arguments to update the system date
 */
void date(char* args);
/**
 * On compilation, the program captures the current date and stores it 
 * into a char* for when version is called to show compilation date.
*/
char* compilation_date();