/**
 * Either updates the time or simply prints the current time
 * @param args Unparsed time arguments to update the system time
 */
void time(char* args);
/**
 * On compilation, the program captures the current time and stores it 
 * into a char* for when version is called to show compilation time.
*/
char* compilation_time();