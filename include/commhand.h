/**
 * Prompts the user to enter commands potentially with parameters
 * then processes any extra spaces anywhere within the command
 * then executes the specified command then repeats indefinitely
 */
void commhand();